---
ID:             026
TYPE:           feature
NAME:           CV import from PDF/DOCX
SUMMARY:        Parse existing CV documents into the section model
STATUS:         OPEN
TAGS:           future, import
---

## Description

Upload an existing CV (PDF or DOCX) and parse it into the editor's
section model. Uses document parsing (text extraction → structured data
mapping). User can then edit and re-export with a different template.
